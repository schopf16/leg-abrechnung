"""Domain logic: distribution engine, billing rules, demo data generation.

This package knows nothing about SQLite row shapes beyond what the
`app.models` repositories expose, and nothing about the GUI or PDF output.
"""
